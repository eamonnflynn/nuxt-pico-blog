{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "clean": "rm -rf .nuxt dist .output"
  },
  "devDependencies": {
    "@nuxt/content": "^2.3.0",
    
    "nuxt": "^3.0.0",
    "typescript":"^4.9.4" 
  },
  "dependencies": {
    "@picocss/pico": "picocss/pico"
  }
}
